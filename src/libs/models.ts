export interface NavbarDataProps {
  path: string;
  route: string;
}

export interface FooterDataProps {
  name: string;
  icon: string;
}

export interface RecentChatsProps {
  title: string;
  icon: string;
  date: string;
}
